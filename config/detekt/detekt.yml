style:

  NoTabs:
    # I'm a tab guy, I don't pay for space in advance.
    active: false

  SpacingAfterPackageAndImports:
    excludes:
      # False positives in gradle/plugins.
      - '**/*.gradle.kts'

<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	>

	<!--
		android_libs.ub_uiautomator=18
		Not using it in this module, but on classpath,
		lint will hopefully find the usages and notice that it's not callable from everywhere.
	-->
	<uses-sdk tools:overrideLibrary="androidx.test.uiautomator" />

	<application
		android:supportsRtl="true"
		>

		<activity
			android:name="net.twisterrob.android.activity.CaptureImage"
			android:theme="@style/Theme.AppCompat"
			/>

	</application>

</manifest>

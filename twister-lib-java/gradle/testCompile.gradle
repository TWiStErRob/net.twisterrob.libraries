dependencies {
	testImplementation(libs.test.junit)
	testImplementation "net.twisterrob.lib:twister-lib-hamcrest"
	testImplementation "net.twisterrob.lib:twister-lib-mockito"
	testImplementation "net.twisterrob.lib:twister-lib-junit4"
	testImplementation "net.twisterrob.lib:twister-lib-test"
	testImplementation(libs.test.hamcrest.junit)
	testImplementation(libs.test.shazamcrest) { // assertThat(*, sameBeanAs(*))
		exclude group: 'junit'
		exclude module: 'guava'
	}
	testImplementation(libs.test.mockito)
	testImplementation(libs.test.powermock.mockito2)
	testImplementation(libs.test.powermock.junit4)
	testImplementation(libs.test.powermock.junit4Rule)
	// Contains duplicate PowerMockRule which messes things up, do not enable.
	testImplementation(libs.test.powermock.junit4Agent)
	testImplementation(libs.test.powermock.xstream)
	testImplementation(libs.test.gwen) // Gwen.given, Gwen.when, Gwen.then
	testRuntimeOnly(libs.slf4j.simple)
	testImplementation(libs.test.params)
}

name: "🧪 Instrumentation Test Matrix"

on:
  workflow_call:

jobs:

  instrumentation:
    name: "${{ matrix.api }}"

    uses: TWiStErRob/github-workflows/.github/workflows/instrumentation.yml@b2ac1bf35e26a7042bbedcd6815faaee3f20a2a8 # v3
    with:
      android-api: ${{ matrix.api }}
      timeout-minutes: 60
      java-version: 21
      # language=bash
      script: |
        ./gradlew --no-daemon --no-configuration-cache --no-build-cache --continue --stacktrace connectedCheck :mergeAndroidReports

    permissions:
      contents: read
      checks: write
      statuses: write

    strategy:
      fail-fast: false
      matrix:
        # The API level, see https://apilevels.com/.
        api:
          - 21 # A stable version for now.
